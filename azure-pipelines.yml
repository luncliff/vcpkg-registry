
trigger:
- main

pool:
  vmImage: windows-latest

steps:
- powershell: cmake --version

- task: run-vcpkg@0
  inputs:
    vcpkgGitCommitId: '2021.05.12'
    vcpkgArguments: '--overlay-ports $(Build.SourcesDirectory)/ports --overlay-triplets $(Build.SourcesDirectory)/triplets lua[cpp]'
