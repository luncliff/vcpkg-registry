diff --git a/src/Agent.cc b/src/Agent2D.cc
similarity index 97%
rename from src/Agent.cc
rename to src/Agent2D.cc
index 92c41b4..e192df5 100644
--- a/src/Agent.cc
+++ b/src/Agent2D.cc
@@ -1,5 +1,5 @@
 /*
- * Agent.cc
+ * Agent2D.cc
  * RVO2 Library
  *
  * SPDX-FileCopyrightText: 2008 University of North Carolina at Chapel Hill
@@ -32,11 +32,11 @@
  */
 
 /**
- * @file  Agent.cc
- * @brief Defines the Agent class.
+ * @file  Agent2D.cc
+ * @brief Defines the Agent2D class.
  */
 
-#include "Agent.h"
+#include "Agent2D.h"
 
 #include <algorithm>
 #include <cmath>
@@ -48,7 +48,7 @@
 namespace RVO {
 namespace {
 /**
- * @relates        Agent
+ * @relates        Agent2D
  * @brief          Solves a one-dimensional linear program on a specified line
  *                 subject to linear constraints defined by lines and a circular
  *                 constraint.
@@ -132,7 +132,7 @@ bool linearProgram1(const std::vector<Line> &lines, std::size_t lineNo,
 }
 
 /**
- * @relates        Agent
+ * @relates        Agent2D
  * @brief          Solves a two-dimensional linear program subject to linear
  *                 constraints defined by lines and a circular constraint.
  * @param[in]      lines        Lines defining the linear constraints.
@@ -177,7 +177,7 @@ std::size_t linearProgram2(const std::vector<Line> &lines, float radius,
 }
 
 /**
- * @relates        Agent
+ * @relates        Agent2D
  * @brief          Solves a two-dimensional linear program subject to linear
  *                 constraints defined by lines and a circular constraint.
  * @param[in]      lines        Lines defining the linear constraints.
@@ -242,7 +242,7 @@ void linearProgram3(const std::vector<Line> &lines, std::size_t numObstLines,
 }
 } /* namespace */
 
-Agent::Agent()
+Agent2D::Agent2D()
     : id_(0U),
       maxNeighbors_(0U),
       maxSpeed_(0.0F),
@@ -251,9 +251,9 @@ Agent::Agent()
       timeHorizon_(0.0F),
       timeHorizonObst_(0.0F) {}
 
-Agent::~Agent() {}
+Agent2D::~Agent2D() {}
 
-void Agent::computeNeighbors(const KdTree *kdTree) {
+void Agent2D::computeNeighbors(const KdTree *kdTree) {
   obstacleNeighbors_.clear();
   const float range = timeHorizonObst_ * maxSpeed_ + radius_;
   kdTree->computeObstacleNeighbors(this, range * range);
@@ -267,7 +267,7 @@ void Agent::computeNeighbors(const KdTree *kdTree) {
 }
 
 /* Search for the best new velocity. */
-void Agent::computeNewVelocity(float timeStep) {
+void Agent2D::computeNewVelocity(float timeStep) {
   orcaLines_.clear();
 
   const float invTimeHorizonObst = 1.0F / timeHorizonObst_;
@@ -540,7 +540,7 @@ void Agent::computeNewVelocity(float timeStep) {
 
   /* Create agent ORCA lines. */
   for (std::size_t i = 0U; i < agentNeighbors_.size(); ++i) {
-    const Agent *const other = agentNeighbors_[i].second;
+    const Agent2D *const other = agentNeighbors_[i].second;
 
     const Vector2 relativePosition = other->position_ - position_;
     const Vector2 relativeVelocity = velocity_ - other->velocity_;
@@ -616,7 +616,7 @@ void Agent::computeNewVelocity(float timeStep) {
   }
 }
 
-void Agent::insertAgentNeighbor(const Agent *agent, float &rangeSq) {
+void Agent2D::insertAgentNeighbor(const Agent2D *agent, float &rangeSq) {
   if (this != agent) {
     const float distSq = absSq(position_ - agent->position_);
 
@@ -641,7 +641,7 @@ void Agent::insertAgentNeighbor(const Agent *agent, float &rangeSq) {
   }
 }
 
-void Agent::insertObstacleNeighbor(const Obstacle *obstacle, float rangeSq) {
+void Agent2D::insertObstacleNeighbor(const Obstacle *obstacle, float rangeSq) {
   const Obstacle *const nextObstacle = obstacle->next_;
 
   float distSq = 0.0F;
@@ -672,7 +672,7 @@ void Agent::insertObstacleNeighbor(const Obstacle *obstacle, float rangeSq) {
   }
 }
 
-void Agent::update(float timeStep) {
+void Agent2D::update(float timeStep) {
   velocity_ = newVelocity_;
   position_ += velocity_ * timeStep;
 }
diff --git a/src/Agent.h b/src/Agent2D.h
similarity index 91%
rename from src/Agent.h
rename to src/Agent2D.h
index 987afdf..cbea36e 100644
--- a/src/Agent.h
+++ b/src/Agent2D.h
@@ -1,5 +1,5 @@
 /*
- * Agent.h
+ * Agent2D.h
  * RVO2 Library
  *
  * SPDX-FileCopyrightText: 2008 University of North Carolina at Chapel Hill
@@ -35,8 +35,8 @@
 #define RVO_AGENT_H_
 
 /**
- * @file  Agent.h
- * @brief Declares the Agent class.
+ * @file  Agent2D.h
+ * @brief Declares the Agent2D class.
  */
 
 #include <cstddef>
@@ -53,17 +53,17 @@ class Obstacle;
 /**
  * @brief Defines an agent in the simulation.
  */
-class Agent {
+class Agent2D {
  private:
   /**
    * @brief Constructs an agent instance.
    */
-  Agent();
+  Agent2D();
 
   /**
    * @brief Destroys this agent instance.
    */
-  ~Agent();
+  ~Agent2D();
 
   /**
    * @brief     Computes the neighbors of this agent.
@@ -84,7 +84,7 @@ class Agent {
    * @param[in]      agent   A pointer to the agent to be inserted.
    * @param[in, out] rangeSq The squared range around this agent.
    */
-  void insertAgentNeighbor(const Agent *agent,
+  void insertAgentNeighbor(const Agent2D *agent,
                            float &rangeSq); /* NOLINT(runtime/references) */
 
   /**
@@ -103,12 +103,12 @@ class Agent {
   void update(float timeStep);
 
   /* Not implemented. */
-  Agent(const Agent &other);
+  Agent2D(const Agent2D &other);
 
   /* Not implemented. */
-  Agent &operator=(const Agent &other);
+  Agent2D &operator=(const Agent2D &other);
 
-  std::vector<std::pair<float, const Agent *> > agentNeighbors_;
+  std::vector<std::pair<float, const Agent2D *> > agentNeighbors_;
   std::vector<std::pair<float, const Obstacle *> > obstacleNeighbors_;
   std::vector<Line> orcaLines_;
   Vector2 newVelocity_;
diff --git a/src/BUILD.bazel b/src/BUILD.bazel
index 38b4a78..bb66f10 100644
--- a/src/BUILD.bazel
+++ b/src/BUILD.bazel
@@ -119,8 +119,8 @@ filegroup(
 cc_library(
     name = "RVO",
     srcs = [
-        "Agent.cc",
-        "Agent.h",
+        "Agent2D.cc",
+        "Agent2D.h",
         "Export.cc",
         "KdTree.cc",
         "KdTree.h",
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 871941b..9caf86e 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -41,8 +41,8 @@ set(RVO_HEADERS
   Vector2.h)
 
 set(RVO_SOURCES
-  Agent.cc
-  Agent.h
+  Agent2D.cc
+  Agent2D.h
   Export.cc
   KdTree.cc
   KdTree.h
@@ -112,6 +112,7 @@ install(TARGETS ${RVO_LIBRARY}
   INCLUDES DESTINATION ${RVO_INCLUDE_DIR})
 
 install(FILES "${CMAKE_CURRENT_BINARY_DIR}/Export.h"
+              "${CMAKE_CURRENT_LIST_DIR}/Agent2D.h"
   COMPONENT development
   DESTINATION ${RVO_INCLUDE_DIR})
 
diff --git a/src/KdTree.cc b/src/KdTree.cc
index d1d4f01..815e65e 100644
--- a/src/KdTree.cc
+++ b/src/KdTree.cc
@@ -41,7 +41,7 @@
 #include <algorithm>
 #include <utility>
 
-#include "Agent.h"
+#include "Agent2D.h"
 #include "Obstacle.h"
 #include "RVOSimulator.h"
 #include "Vector2.h"
@@ -371,11 +371,11 @@ KdTree::ObstacleTreeNode *KdTree::buildObstacleTreeRecursive(
   return NULL;
 }
 
-void KdTree::computeAgentNeighbors(Agent *agent, float &rangeSq) const {
+void KdTree::computeAgentNeighbors(Agent2D *agent, float &rangeSq) const {
   queryAgentTreeRecursive(agent, rangeSq, 0U);
 }
 
-void KdTree::computeObstacleNeighbors(Agent *agent, float rangeSq) const {
+void KdTree::computeObstacleNeighbors(Agent2D *agent, float rangeSq) const {
   queryObstacleTreeRecursive(agent, rangeSq, obstacleTree_);
 }
 
@@ -387,7 +387,7 @@ void KdTree::deleteObstacleTree(ObstacleTreeNode *node) {
   }
 }
 
-void KdTree::queryAgentTreeRecursive(Agent *agent, float &rangeSq,
+void KdTree::queryAgentTreeRecursive(Agent2D *agent, float &rangeSq,
                                      std::size_t node) const {
   if (agentTree_[node].end - agentTree_[node].begin <= RVO_MAX_LEAF_SIZE) {
     for (std::size_t i = agentTree_[node].begin; i < agentTree_[node].end;
@@ -439,7 +439,7 @@ void KdTree::queryAgentTreeRecursive(Agent *agent, float &rangeSq,
   }
 }
 
-void KdTree::queryObstacleTreeRecursive(Agent *agent, float rangeSq,
+void KdTree::queryObstacleTreeRecursive(Agent2D *agent, float rangeSq,
                                         const ObstacleTreeNode *node) const {
   if (node != NULL) {
     const Obstacle *const obstacle1 = node->obstacle;
diff --git a/src/KdTree.h b/src/KdTree.h
index e50b289..e333ed3 100644
--- a/src/KdTree.h
+++ b/src/KdTree.h
@@ -43,7 +43,7 @@
 #include <vector>
 
 namespace RVO {
-class Agent;
+class Agent2D;
 class Obstacle;
 class RVOSimulator;
 class Vector2;
@@ -101,7 +101,7 @@ class KdTree {
    * @param[in, out] rangeSq The squared range around the agent.
    */
   void computeAgentNeighbors(
-      Agent *agent, float &rangeSq) const; /* NOLINT(runtime/references) */
+      Agent2D *agent, float &rangeSq) const; /* NOLINT(runtime/references) */
 
   /**
    * @brief     Computes the obstacle neighbors of the specified agent.
@@ -109,7 +109,7 @@ class KdTree {
    *                    to be computed.
    * @param[in] rangeSq The squared range around the agent.
    */
-  void computeObstacleNeighbors(Agent *agent, float rangeSq) const;
+  void computeObstacleNeighbors(Agent2D *agent, float rangeSq) const;
 
   /**
    * @brief     Deletes the specified obstacle tree node.
@@ -125,7 +125,7 @@ class KdTree {
    * @param[in,out] rangeSq The squared range around the agent.
    * @param[in]     node    The current agent k-D tree node.
    */
-  void queryAgentTreeRecursive(Agent *agent,
+  void queryAgentTreeRecursive(Agent2D *agent,
                                float &rangeSq, /* NOLINT(runtime/references) */
                                std::size_t node) const;
 
@@ -137,7 +137,7 @@ class KdTree {
    * @param[in,out] rangeSq The squared range around the agent.
    * @param[in]     node    The current obstacle k-D tree node.
    */
-  void queryObstacleTreeRecursive(Agent *agent, float rangeSq,
+  void queryObstacleTreeRecursive(Agent2D *agent, float rangeSq,
                                   const ObstacleTreeNode *node) const;
 
   /**
@@ -176,12 +176,12 @@ class KdTree {
   /* Not implemented. */
   KdTree &operator=(const KdTree &other);
 
-  std::vector<Agent *> agents_;
+  std::vector<Agent2D *> agents_;
   std::vector<AgentTreeNode> agentTree_;
   ObstacleTreeNode *obstacleTree_;
   RVOSimulator *simulator_;
 
-  friend class Agent;
+  friend class Agent2D;
   friend class RVOSimulator;
 };
 } /* namespace RVO */
diff --git a/src/Obstacle.h b/src/Obstacle.h
index e2008a7..6e896ef 100644
--- a/src/Obstacle.h
+++ b/src/Obstacle.h
@@ -72,7 +72,7 @@ class Obstacle {
   std::size_t id_;
   bool isConvex_;
 
-  friend class Agent;
+  friend class Agent2D;
   friend class KdTree;
   friend class RVOSimulator;
 };
diff --git a/src/RVO.h b/src/RVO.h
index 76f96de..35d6fda 100644
--- a/src/RVO.h
+++ b/src/RVO.h
@@ -359,7 +359,7 @@ bool reachedGoal(RVO::RVOSimulator *simulator) {
   for (std::size_t i = 0U; i < simulator->getNumAgents(); ++i) {
     if (absSq(goals[i] - simulator->getAgentPosition(i)) >
         simulator->getAgentRadius(i) * simulator->getAgentRadius(i)) {
-      // Agent is further away from its goal than one radius.
+      // Agent2D is further away from its goal than one radius.
       return false;
     }
   }
@@ -408,10 +408,10 @@ void setPreferredVelocities(RVO::RVOSimulator *simulator) {
   for (std::size_t i = 0U; i < simulator->getNumAgents(); ++i) {
     if (absSq(goals[i] - simulator->getAgentPosition(i)) <
         simulator->getAgentRadius(i) * simulator->getAgentRadius(i) ) {
-      // Agent is within one radius of its goal, set preferred velocity to zero.
+      // Agent2D is within one radius of its goal, set preferred velocity to zero.
       simulator->setAgentPrefVelocity(i, RVO::Vector2(0.0F, 0.0F));
     } else {
-      // Agent is far away from its goal, set preferred velocity as unit vector
+      // Agent2D is far away from its goal, set preferred velocity as unit vector
       // towards agent's goal.
       simulator->setAgentPrefVelocity(i,
                                 normalize(goals[i] -
@@ -467,7 +467,7 @@ around the obstacles.</td>
 </tr>
 </table>
 
-@section agent_parameters Agent Parameters
+@section agent_parameters Agent2D Parameters
 
 <table border="0" cellpadding="3" width="100%">
 <tr>
diff --git a/src/RVOSimulator.cc b/src/RVOSimulator.cc
index 1ecf171..1471379 100644
--- a/src/RVOSimulator.cc
+++ b/src/RVOSimulator.cc
@@ -41,7 +41,7 @@
 #include <limits>
 #include <utility>
 
-#include "Agent.h"
+#include "Agent2D.h"
 #include "KdTree.h"
 #include "Line.h"
 #include "Obstacle.h"
@@ -63,7 +63,7 @@ RVOSimulator::RVOSimulator()
 RVOSimulator::RVOSimulator(float timeStep, float neighborDist,
                            std::size_t maxNeighbors, float timeHorizon,
                            float timeHorizonObst, float radius, float maxSpeed)
-    : defaultAgent_(new Agent()),
+    : defaultAgent_(new Agent2D()),
       kdTree_(new KdTree(this)),
       globalTime_(0.0F),
       timeStep_(timeStep) {
@@ -79,7 +79,7 @@ RVOSimulator::RVOSimulator(float timeStep, float neighborDist,
                            std::size_t maxNeighbors, float timeHorizon,
                            float timeHorizonObst, float radius, float maxSpeed,
                            const Vector2 &velocity)
-    : defaultAgent_(new Agent()),
+    : defaultAgent_(new Agent2D()),
       kdTree_(new KdTree(this)),
       globalTime_(0.0F),
       timeStep_(timeStep) {
@@ -107,7 +107,7 @@ RVOSimulator::~RVOSimulator() {
 
 std::size_t RVOSimulator::addAgent(const Vector2 &position) {
   if (defaultAgent_ != NULL) {
-    Agent *const agent = new Agent();
+    Agent2D *const agent = new Agent2D();
     agent->position_ = position;
     agent->velocity_ = defaultAgent_->velocity_;
     agent->id_ = agents_.size();
@@ -137,7 +137,7 @@ std::size_t RVOSimulator::addAgent(const Vector2 &position, float neighborDist,
                                    std::size_t maxNeighbors, float timeHorizon,
                                    float timeHorizonObst, float radius,
                                    float maxSpeed, const Vector2 &velocity) {
-  Agent *const agent = new Agent();
+  Agent2D *const agent = new Agent2D();
   agent->position_ = position;
   agent->velocity_ = velocity;
   agent->id_ = agents_.size();
@@ -315,7 +315,7 @@ void RVOSimulator::setAgentDefaults(float neighborDist,
                                     float timeHorizonObst, float radius,
                                     float maxSpeed, const Vector2 &velocity) {
   if (defaultAgent_ == NULL) {
-    defaultAgent_ = new Agent();
+    defaultAgent_ = new Agent2D();
   }
 
   defaultAgent_->maxNeighbors_ = maxNeighbors;
diff --git a/src/RVOSimulator.h b/src/RVOSimulator.h
index 46e3414..f7ab8d9 100644
--- a/src/RVOSimulator.h
+++ b/src/RVOSimulator.h
@@ -45,7 +45,7 @@
 #include "Export.h"
 
 namespace RVO {
-class Agent;
+class Agent2D;
 class KdTree;
 class Line;
 class Obstacle;
@@ -652,9 +652,9 @@ class RVO_EXPORT RVOSimulator {
   /* Not implemented. */
   RVOSimulator &operator=(const RVOSimulator &other);
 
-  std::vector<Agent *> agents_;
+  std::vector<Agent2D *> agents_;
   std::vector<Obstacle *> obstacles_;
-  Agent *defaultAgent_;
+  Agent2D *defaultAgent_;
   KdTree *kdTree_;
   float globalTime_;
   float timeStep_;
