if(VCPKG_TARGET_IS_ANDROID)
    # ${SOURCE_PATH}/Configuration/15-android.conf
    if(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm64")
        set(OPENSSL_PLATFORM "android-arm64")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm")
        set(OPENSSL_PLATFORM "android-arm")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "x64")
        set(OPENSSL_PLATFORM "android-x86_64")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "x86")
        set(OPENSSL_PLATFORM "android-x86")
    endif()

elseif(VCPKG_TARGET_IS_LINUX)
    if(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm64")
        set(OPENSSL_PLATFORM "linux-aarch64")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm")
        set(OPENSSL_PLATFORM "linux-armv4")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "x64")
        set(OPENSSL_PLATFORM "linux-x86_64")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "x86")
        set(OPENSSL_PLATFORM "linux-x86")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "ppc64le")
        set(OPENSSL_PLATFORM "linux-ppc64le")
    endif()

elseif(VCPKG_TARGET_IS_IOS)
    # ${SOURCE_PATH}/Configuration/15-ios.conf
    if(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm64")
        set(OPENSSL_PLATFORM "ios64-xcrun")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm")
        set(OPENSSL_PLATFORM "ios-xcrun")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "x86" OR
           VCPKG_TARGET_ARCHITECTURE STREQUAL "x64")
        set(OPENSSL_PLATFORM "iossimulator-xcrun")
    endif()

elseif(VCPKG_TARGET_IS_OSX)
    if(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm64")
        set(OPENSSL_PLATFORM "darwin64-arm64-cc")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "x64")
        set(OPENSSL_PLATFORM "darwin64-x86_64-cc")
    endif()

elseif(VCPKG_TARGET_IS_FREEBSD OR VCPKG_TARGET_IS_OPENBSD)
    if(VCPKG_TARGET_ARCHITECTURE STREQUAL "x64")
        set(OPENSSL_PLATFORM "BSD-x86_64")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "x86")
        set(OPENSSL_PLATFORM "BSD-x86")
    endif()

elseif(VCPKG_TARGET_IS_MINGW)
    if(VCPKG_TARGET_ARCHITECTURE STREQUAL "x64")
        set(OPENSSL_PLATFORM "mingw64")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "x86")
        set(OPENSSL_PLATFORM "mingw")
    endif()

elseif(VCPKG_TARGET_IS_UWP)
    # ${SOURCE_PATH}/Configuration/50-win-onecore.conf
    if(VCPKG_TARGET_ARCHITECTURE STREQUAL "x86")
        set(OPENSSL_PLATFORM "VC-WIN32-UWP")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "x64")
        set(OPENSSL_PLATFORM "VC-WIN64A-UWP")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm")
        set(OPENSSL_PLATFORM "VC-WIN32-ARM-UWP")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm64")
        set(OPENSSL_PLATFORM "VC-WIN64-ARM-UWP")
    endif()

elseif(VCPKG_TARGET_IS_WINDOWS)
    # ${SOURCE_PATH}/Configuration/50-win-onecore.conf
    if(VCPKG_TARGET_ARCHITECTURE STREQUAL "x86")
        set(OPENSSL_PLATFORM "VC-WIN32")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "x64")
        set(OPENSSL_PLATFORM "VC-WIN64A")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm")
        set(OPENSSL_PLATFORM "VC-WIN32-ARM")
    elseif(VCPKG_TARGET_ARCHITECTURE STREQUAL "arm64")
        set(OPENSSL_PLATFORM "VC-WIN64-ARM")
    endif()

endif()

if(NOT DEFINED OPENSSL_PLATFORM)
    message(FATAL_ERROR "OPENSSL_PLATFORM is unknown for the target OPENSSL_PLATFORM/architecture")
endif()