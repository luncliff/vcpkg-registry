diff --git a/src/Agent.cc b/src/Agent3D.cc
similarity index 97%
rename from src/Agent.cc
rename to src/Agent3D.cc
index c796bc6..19a9a0a 100644
--- a/src/Agent.cc
+++ b/src/Agent3D.cc
@@ -1,5 +1,5 @@
 /*
- * Agent.cc
+ * Agent3D.cc
  * RVO2-3D Library
  *
  * SPDX-FileCopyrightText: 2008 University of North Carolina at Chapel Hill
@@ -31,7 +31,7 @@
  * <https://gamma.cs.unc.edu/RVO2/>
  */
 
-#include "Agent.h"
+#include "Agent3D.h"
 
 #include <algorithm>
 #include <cmath>
@@ -354,7 +354,7 @@ void linearProgram4(const std::vector<Plane> &planes, std::size_t beginPlane,
 }
 } /* namespace */
 
-Agent::Agent(RVOSimulator *sim)
+Agent3D::Agent3D(RVOSimulator *sim)
     : sim_(sim),
       id_(0U),
       maxNeighbors_(0U),
@@ -363,9 +363,9 @@ Agent::Agent(RVOSimulator *sim)
       radius_(0.0F),
       timeHorizon_(0.0F) {}
 
-Agent::~Agent() {}
+Agent3D::~Agent3D() {}
 
-void Agent::computeNeighbors() {
+void Agent3D::computeNeighbors() {
   agentNeighbors_.clear();
 
   if (maxNeighbors_ > 0U) {
@@ -373,13 +373,13 @@ void Agent::computeNeighbors() {
   }
 }
 
-void Agent::computeNewVelocity() {
+void Agent3D::computeNewVelocity() {
   orcaPlanes_.clear();
   const float invTimeHorizon = 1.0F / timeHorizon_;
 
   /* Create agent ORCA planes. */
   for (std::size_t i = 0U; i < agentNeighbors_.size(); ++i) {
-    const Agent *const other = agentNeighbors_[i].second;
+    const Agent3D *const other = agentNeighbors_[i].second;
     const Vector3 relativePosition = other->position_ - position_;
     const Vector3 relativeVelocity = velocity_ - other->velocity_;
     const float distSq = absSq(relativePosition);
@@ -443,7 +443,7 @@ void Agent::computeNewVelocity() {
   }
 }
 
-void Agent::insertAgentNeighbor(const Agent *agent, float &rangeSq) {
+void Agent3D::insertAgentNeighbor(const Agent3D *agent, float &rangeSq) {
   if (this != agent) {
     const float distSq = absSq(position_ - agent->position_);
 
@@ -468,7 +468,7 @@ void Agent::insertAgentNeighbor(const Agent *agent, float &rangeSq) {
   }
 }
 
-void Agent::update() {
+void Agent3D::update() {
   velocity_ = newVelocity_;
   position_ += velocity_ * sim_->timeStep_;
 }
diff --git a/src/Agent.h b/src/Agent3D.h
similarity index 88%
rename from src/Agent.h
rename to src/Agent3D.h
index 3602775..3a43812 100644
--- a/src/Agent.h
+++ b/src/Agent3D.h
@@ -1,5 +1,5 @@
 /*
- * Agent.h
+ * Agent3D.h
  * RVO2-3D Library
  *
  * SPDX-FileCopyrightText: 2008 University of North Carolina at Chapel Hill
@@ -35,8 +35,8 @@
 #define RVO3D_AGENT_H_
 
 /**
- * @file  Agent.h
- * @brief Contains the Agent class.
+ * @file  Agent3D.h
+ * @brief Contains the Agent3D class.
  */
 
 #include <cstddef>
@@ -52,18 +52,18 @@ class RVOSimulator;
 /**
  * @brief Defines an agent in the simulation.
  */
-class Agent {
+class Agent3D {
  private:
   /**
    * @brief     Constructs an agent instance.
    * @param[in] sim The simulator instance.
    */
-  explicit Agent(RVOSimulator *sim);
+  explicit Agent3D(RVOSimulator *sim);
 
   /**
    * @brief Destroys this agent instance.
    */
-  ~Agent();
+  ~Agent3D();
 
   /**
    * @brief Computes the neighbors of this agent.
@@ -81,7 +81,7 @@ class Agent {
    * @param[in] agent   A pointer to the agent to be inserted.
    * @param[in] rangeSq The squared range around this agent.
    */
-  void insertAgentNeighbor(const Agent *agent,
+  void insertAgentNeighbor(const Agent3D *agent,
                            float &rangeSq); /* NOLINT(runtime/references) */
 
   /**
@@ -91,10 +91,10 @@ class Agent {
   void update();
 
   /* Not implemented. */
-  Agent(const Agent &other);
+  Agent3D(const Agent3D &other);
 
   /* Not implemented. */
-  Agent &operator=(const Agent &other);
+  Agent3D &operator=(const Agent3D &other);
 
   Vector3 newVelocity_;
   Vector3 position_;
@@ -107,7 +107,7 @@ class Agent {
   float neighborDist_;
   float radius_;
   float timeHorizon_;
-  std::vector<std::pair<float, const Agent *> > agentNeighbors_;
+  std::vector<std::pair<float, const Agent3D *> > agentNeighbors_;
   std::vector<Plane> orcaPlanes_;
 
   friend class KdTree;
diff --git a/src/BUILD.bazel b/src/BUILD.bazel
index 579325d..99248c4 100644
--- a/src/BUILD.bazel
+++ b/src/BUILD.bazel
@@ -119,8 +119,8 @@ filegroup(
 cc_library(
     name = "RVO3D",
     srcs = [
-        "Agent.cc",
-        "Agent.h",
+        "Agent3D.cc",
+        "Agent3D.h",
         "Export.cc",
         "KdTree.cc",
         "KdTree.h",
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 262b21c..ab56229 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -41,8 +41,8 @@ set(RVO3D_HEADERS
   Vector3.h)
 
 set(RVO3D_SOURCES
-  Agent.cc
-  Agent.h
+  Agent3D.cc
+  Agent3D.h
   Export.cc
   KdTree.cc
   KdTree.h
@@ -111,6 +111,7 @@ install(TARGETS ${RVO3D_LIBRARY}
   INCLUDES DESTINATION ${RVO3D_INCLUDE_DIR})
 
 install(FILES "${CMAKE_CURRENT_BINARY_DIR}/Export.h"
+              "${CMAKE_CURRENT_LIST_DIR}/Agent3D.h"
   COMPONENT development
   DESTINATION ${RVO3D_INCLUDE_DIR})
 
diff --git a/src/KdTree.cc b/src/KdTree.cc
index 7913842..bd7f539 100644
--- a/src/KdTree.cc
+++ b/src/KdTree.cc
@@ -36,7 +36,7 @@
 #include <algorithm>
 #include <utility>
 
-#include "Agent.h"
+#include "Agent3D.h"
 #include "RVOSimulator.h"
 #include "Vector3.h"
 
@@ -182,11 +182,11 @@ void KdTree::buildAgentTreeRecursive(std::size_t begin, std::size_t end,
   }
 }
 
-void KdTree::computeAgentNeighbors(Agent *agent, float rangeSq) const {
+void KdTree::computeAgentNeighbors(Agent3D *agent, float rangeSq) const {
   queryAgentTreeRecursive(agent, rangeSq, 0U);
 }
 
-void KdTree::queryAgentTreeRecursive(Agent *agent, float &rangeSq,
+void KdTree::queryAgentTreeRecursive(Agent3D *agent, float &rangeSq,
                                      std::size_t node) const {
   if (agentTree_[node].end - agentTree_[node].begin <= RVO3D_MAX_LEAF_SIZE) {
     for (std::size_t i = agentTree_[node].begin; i < agentTree_[node].end;
diff --git a/src/KdTree.h b/src/KdTree.h
index 707e47a..2ea8066 100644
--- a/src/KdTree.h
+++ b/src/KdTree.h
@@ -43,7 +43,7 @@
 #include <vector>
 
 namespace RVO {
-class Agent;
+class Agent3D;
 class RVOSimulator;
 
 /**
@@ -84,7 +84,7 @@ class KdTree {
    *                    be computed.
    * @param[in] rangeSq The squared range around the agent.
    */
-  void computeAgentNeighbors(Agent *agent, float rangeSq) const;
+  void computeAgentNeighbors(Agent3D *agent, float rangeSq) const;
 
   /**
    * @brief         Recursive function to compute the neighbors of the specified
@@ -95,7 +95,7 @@ class KdTree {
    * @param[in]     node    The current k-D tree node.
    */
 
-  void queryAgentTreeRecursive(Agent *agent,
+  void queryAgentTreeRecursive(Agent3D *agent,
                                float &rangeSq, /* NOLINT(runtime/references) */
                                std::size_t node) const;
 
@@ -105,11 +105,11 @@ class KdTree {
   /* Not implemented. */
   KdTree &operator=(const KdTree &other);
 
-  std::vector<Agent *> agents_;
+  std::vector<Agent3D *> agents_;
   std::vector<AgentTreeNode> agentTree_;
   RVOSimulator *sim_;
 
-  friend class Agent;
+  friend class Agent3D;
   friend class RVOSimulator;
 };
 } /* namespace RVO */
diff --git a/src/RVO.h b/src/RVO.h
index d669954..581a30e 100644
--- a/src/RVO.h
+++ b/src/RVO.h
@@ -234,7 +234,7 @@ bool reachedGoal(RVO::RVOSimulator *sim,
   for (std::size_t i = 0U; i < sim->getNumAgents(); ++i) {
     if (absSq(goals[i] - sim->getAgentPosition(i)) > sim->getAgentRadius(i) *
         sim->getAgentRadius(i)) {
-      // Agent is further away from its goal than one radius.
+      // Agent3D is further away from its goal than one radius.
       return false;
     }
   }
@@ -273,10 +273,10 @@ void setPreferredVelocities(RVO::RVOSimulator *sim,
   for (std::size_t i = 0U; i < sim->getNumAgents(); ++i) {
     if (absSq(goals[i] - sim->getAgentPosition(i)) < sim->getAgentRadius(i) *
         sim->getAgentRadius(i) ) {
-      // Agent is within one radius of its goal, set preferred velocity to zero.
+      // Agent3D is within one radius of its goal, set preferred velocity to zero.
       sim->setAgentPrefVelocity(i, RVO::Vector3());
     } else {
-      // Agent is far away from its goal, set preferred velocity as unit vector
+      // Agent3D is far away from its goal, set preferred velocity as unit vector
       // towards agent's goal.
       sim->setAgentPrefVelocity(i, normalize(goals[i] -
       sim->getAgentPosition(i)));
@@ -316,7 +316,7 @@ distributed on a sphere, move to the antipodal position on the sphere.</td>
 </tr>
 </table>
 
-@section agent Agent Parameters
+@section agent Agent3D Parameters
 
 <table border="0" cellpadding="3" width="100%">
 <tr>
diff --git a/src/RVOSimulator.cc b/src/RVOSimulator.cc
index acccd7b..1cc5b9a 100644
--- a/src/RVOSimulator.cc
+++ b/src/RVOSimulator.cc
@@ -39,7 +39,7 @@
 #include <omp.h>
 #endif /* _OPENMP */
 
-#include "Agent.h"
+#include "Agent3D.h"
 #include "KdTree.h"
 #include "Plane.h"
 
@@ -54,7 +54,7 @@ RVOSimulator::RVOSimulator(float timeStep, float neighborDist,
                            std::size_t maxNeighbors, float timeHorizon,
                            float radius, float maxSpeed,
                            const Vector3 &velocity)
-    : defaultAgent_(new Agent(this)),
+    : defaultAgent_(new Agent3D(this)),
       kdTree_(new KdTree(this)),
       globalTime_(0.0F),
       timeStep_(timeStep) {
@@ -104,7 +104,7 @@ std::size_t RVOSimulator::addAgent(const Vector3 &position) {
     return RVO3D_ERROR;
   }
 
-  Agent *agent = new Agent(this);
+  Agent3D *agent = new Agent3D(this);
 
   agent->position_ = position;
   agent->maxNeighbors_ = defaultAgent_->maxNeighbors_;
@@ -125,7 +125,7 @@ std::size_t RVOSimulator::addAgent(const Vector3 &position, float neighborDist,
                                    std::size_t maxNeighbors, float timeHorizon,
                                    float radius, float maxSpeed,
                                    const Vector3 &velocity) {
-  Agent *agent = new Agent(this);
+  Agent3D *agent = new Agent3D(this);
 
   agent->position_ = position;
   agent->maxNeighbors_ = maxNeighbors;
@@ -200,7 +200,7 @@ void RVOSimulator::setAgentDefaults(float neighborDist,
                                     float radius, float maxSpeed,
                                     const Vector3 &velocity) {
   if (defaultAgent_ == NULL) {
-    defaultAgent_ = new Agent(this);
+    defaultAgent_ = new Agent3D(this);
   }
 
   defaultAgent_->maxNeighbors_ = maxNeighbors;
diff --git a/src/RVOSimulator.h b/src/RVOSimulator.h
index af0c1e9..5be73ec 100644
--- a/src/RVOSimulator.h
+++ b/src/RVOSimulator.h
@@ -47,7 +47,7 @@
 #include "Vector3.h"
 
 namespace RVO {
-class Agent;
+class Agent3D;
 class KdTree;
 class Plane;
 
@@ -401,13 +401,13 @@ class RVO3D_EXPORT RVOSimulator {
   /* Not implemented. */
   RVOSimulator &operator=(const RVOSimulator &other);
 
-  Agent *defaultAgent_;
+  Agent3D *defaultAgent_;
   KdTree *kdTree_;
   float globalTime_;
   float timeStep_;
-  std::vector<Agent *> agents_;
+  std::vector<Agent3D *> agents_;
 
-  friend class Agent;
+  friend class Agent3D;
   friend class KdTree;
 };
 } /* namespace RVO */
